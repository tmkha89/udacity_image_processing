{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "node server.js",
    "clean": "rm -rf www/ || true",
    "build": "npm run clean && mkdir www && cp package.json www/package.json && cd www && zip -r Archive.zip ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/bluebird": "^3.5.34",
    "axios": "^1.7.2",
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "jimp": "^0.22.12"
  },
  "devDependencies": {
    "nodemon": "^3.1.1"
  }
}
